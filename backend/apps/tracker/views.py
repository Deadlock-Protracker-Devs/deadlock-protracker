from django.shortcuts import render

# views are functions that respond to an HTTP request
# in our case i think that views will basically perform some database query and encode that data into json so that react can render it
